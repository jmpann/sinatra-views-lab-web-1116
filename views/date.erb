<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% t = DateTime.now.strftime("%A, %B %d, %Y") %>
   <h1><%= "The date is #{t}" %></h1>

  </body>
</html>
